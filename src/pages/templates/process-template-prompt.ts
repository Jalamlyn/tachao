/**
 * 通用流程表单模板提示词
 * 包含通用审批流程相关表单的标准化需求描述
 * 基于 DynamicForm 组件的实际功能进行优化
 */
export const processTemplatePrompt = `这是一个适用于通用审批流程管理的标准化表单模板，支持动态联动和自定义扩展，可满足行政、财务、人事等场景的审批需求。

### 具体需求如下：

---

#### 1. 申请信息
- **基本信息**
  - 申请人（resource，关联员工主数据，必填）
  - 申请部门（resource，关联部门主数据，必填）
  - 申请日期（date，必填）
  - 紧急程度（select，选项：普通/紧急/特急，必填）
  - 申请标题（text，必填）
  - 申请说明（textarea，必填）
  - 预计完成日期（date，选填）
  - 相关附件（file，多文件上传）

- **申请类型**
  - 流程类型（select，必填，选项：行政审批/财务审批/人事审批/其他）
  - 子类型（select，根据流程类型联动显示）
  - 是否需要会签（switch）
  - 会签部门（resource，多选，关联部门主数据）

---

#### 2. 费用明细（如果涉及费用）
- **费用表格**
  - 可添加多行记录，字段包括：
    - 费用类型（select，必填）
    - 费用金额（number，必填）
    - 费用说明（text）
    - 发生日期（date）
    - 发票凭证（file，支持图片上传）
    - 报销金额（number，必填）
    - 实报金额（number，自动计算：报销金额 - 审核减扣金额）

---

#### 3. 审批流程
- **部门主管审批**
  - 审批人（resource，关联员工主数据）
  - 审批时间（datetime，自动记录）
  - 审批结果（radio：同意/不同意/退回）
  - 审批意见（textarea，选填）
  - 附件上传（file，支持多文件）
  - 审批签名（signature，电子签名）

- **分管领导审批**（金额或流程级别超过阈值时显示）
  - 审批人（resource，关联员工主数据）
  - 审批时间（datetime，自动记录）
  - 审批结果（radio：同意/不同意/退回）
  - 审批意见（textarea，选填）
  - 附件上传（file，支持多文件）
  - 审批签名（signature，电子签名）

- **会签审批**（需要会签时显示）
  - 会签部门（resource，关联部门主数据）
  - 会签人（resource，关联员工主数据）
  - 会签时间（datetime，自动记录）
  - 会签意见（textarea）
  - 会签结果（radio：同意/不同意/退回）
  - 会签签名（signature，电子签名）

- **最终审批**
  - 审批人（resource，关联员工主数据）
  - 审批时间（datetime，自动记录）
  - 审批结果（radio：同意/不同意/退回）
  - 审批意见（textarea，选填）
  - 附件上传（file，支持多文件）
  - 审批签名（signature，电子签名）

---

#### 4. 执行反馈
- **执行信息**
  - 执行人（resource，关联员工主数据）
  - 开始时间（datetime）
  - 完成时间（datetime）
  - 执行状态（select：进行中/已完成/暂停/终止）
  - 执行说明（textarea）
  - 相关附件（file，多文件上传）

- **执行进度**
  - 进度比例（slider，范围：0-100）
  - 最新更新时间（datetime）
  - 进度说明（textarea）
  - 遇到的问题（textarea）
  - 解决方案（textarea）

---

### 动态扩展功能
- 根据实际流程需求，模板支持以下动态功能：
  1. **字段联动**：如根据申请类型自动调整子类型选项或费用表格显示规则。
  2. **条件显示**：特定审批步骤（如分管领导审批、会签审批）根据业务逻辑自动显示或隐藏。
  3. **自动计算**：支持报销金额、费用差额等字段的实时动态计算。
  4. **多语言支持**：字段名称和选项支持多语言配置。

### 使用说明
- 此模板适配通用审批流程，适用于行政审批、财务审批等场景。
- 如果有其他需求（如新增审批步骤或自定义字段），请直接描述，我将及时优化模板内容。`

export default processTemplatePrompt
