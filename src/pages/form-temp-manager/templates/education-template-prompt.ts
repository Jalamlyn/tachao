/**
 * 教育培训表单模板提示词
 * 包含教育培训相关表单的标准化需求描述
 * 基于 DynamicForm 组件的实际功能进行优化
 */
export const educationTemplatePrompt = `这是一个用于教育机构的招生、考试和培训管理的综合性表单。

具体需求如下：

1. 学员信息分组
   - 基本信息
     * 学员姓名（text，必填）
     * 性别（radio：男/女）
     * 出生日期（date）
     * 身份证号（text，必填，支持验证）
     * 联系电话（tel，必填）
     * 电子邮箱（email）
     * 现居地址（textarea）
     * 紧急联系人（text）
     * 紧急联系电话（tel）

   - 教育背景
     * 最高学历（select：高中/专科/本科/硕士/博士）
     * 毕业院校（text）
     * 专业方向（text）
     * 毕业时间（date）
     * 学历证书（file）
     * 其他证书（file，支持多文件）

2. 课程报名表格
   - 可添加多行记录，包含字段：
     * 课程名称（resource，关联课程主数据，必填）
     * 课程类型（select：线上/线下/混合）
     * 开课时间（datetime，必填）
     * 结课时间（datetime，必填）
     * 授课教师（resource，关联教师主数据）
     * 课程费用（number，自动获取）
     * 优惠方式（select：无优惠/早鸟优惠/团报优惠/其他）
     * 优惠金额（number）
     * 实付金额（number，自动计算）
     * 报名状态（select：待付款/已付款/已确认/已取消）

3. 考试成绩表格
   - 可添加多行记录，包含字段：
     * 考试科目（resource，关联课程主数据，必填）
     * 考试时间（datetime，必填）
     * 考试形式（select：笔试/机试/口试/实操）
     * 考试地点（text）
     * 监考教师（resource，关联教师主数据）
     * 考试成绩（number，0-100）
     * 及格线（number，默认60）
     * 是否通过（switch，自动判断）
     * 补考次数（number）
     * 考试备注（textarea）

4. 学习进度跟踪
   - 课程进度
     * 课程名称（resource，关联课程主数据）
     * 总课时（number）
     * 已完成课时（number）
     * 进度比例（slider，0-100，自动计算）
     * 学习状态（select：未开始/进行中/已完成/已暂停）
     * 最近学习（datetime）
     * 学习时长（number，单位：小时）
     * 学习记录（textarea）

   - 作业完成
     * 作业名称（text）
     * 发布时间（datetime）
     * 截止时间（datetime）
     * 完成状态（select：未开始/进行中/已提交/已批改）
     * 作业成绩（number，0-100）
     * 教师评语（textarea）
     * 作业附件（file）

5. 证书管理
   - 证书信息
     * 证书名称（text，必填）
     * 证书编号（text，自动生成）
     * 发证日期（date）
     * 有效期限（date）
     * 发证机构（text）
     * 证书等级（select）
     * 证书图片（image）
     * 证书状态（select：有效/即将过期/已过期）

6. 满意度评价
   - 课程评价
     * 评价时间（datetime）
     * 课程内容（rate，1-5星）
     * 教师水平（rate，1-5星）
     * 教学设施（rate，1-5星）
     * 服务态度（rate，1-5星）
     * 总体评价（rate，1-5星）
     * 评价内容（textarea）
     * 建议反馈（textarea）

特殊功能支持：
1. 表单分组展示，使用 Tabs 切换
2. 支持表格数据的动态添加和删除
3. 支持文件上传和预览
4. 支持电子签名
5. 支持资源选择（关联主数据）
6. 支持数字字段的自动计算
7. 支持必填字段实时验证
8. 支持表单打印，生成标准化的 PDF 文档
9. 支持流程确认步骤的状态追踪
10. 支持表单数据的暂存和恢复
11. 支持成绩统计分析
12. 支持学习进度追踪
13. 支持证书自动生成
14. 支持满意度分析
15. 支持考试成绩导出

业务规则：
1. 使用 orderNumberConfig 生成学员编号
2. 使用 validators 进行字段验证
3. 使用 calculate 进行字段计算
4. 使用 processSteps 控制流程确认
5. 使用 watch 实现字段联动
6. 使用 resource 实现主数据关联
7. 使用 signature 实现电子签名
8. 使用 file/image 实现文件上传
9. 支持成绩自动统计
10. 支持学习进度计算
11. 支持证书有效期管理
12. 支持满意度分析
13. 支持考试成绩分析
14. 支持学习记录追踪
15. 支持统计报表导出

上述需求是否满足您的需求？如果不满足您可以告诉我您需要什么功能，我随时调整模板内容`

export default educationTemplatePrompt
