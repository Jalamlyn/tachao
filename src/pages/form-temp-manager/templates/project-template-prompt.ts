/**
 * 项目管理表单模板提示词
 * 包含项目管理相关表单的标准化需求描述
 * 基于 DynamicForm 组件的实际功能进行优化
 */
export const projectTemplatePrompt = `这是一个用于项目立项、进度跟踪和管理的综合性表单。

具体需求如下：

1. 项目基本信息分组
   - 项目概况
     * 项目名称（text，必填）
     * 项目编号（text，自动生成）
     * 项目类型（select，必填，选项：研发项目/实施项目/运维项目/其他）
     * 项目级别（select，必填，选项：重点项目/普通项目/试点项目）
     * 计划开始日期（date，必填）
     * 计划结束日期（date，必填）
     * 项目预算（number，必填）
     * 项目简介（textarea，必填）
     * 项目目标（textarea，必填）
     * 相关文档（file，支持多文件）

   - 项目组织
     * 项目经理（resource，关联员工主数据，必填）
     * 项目部门（resource，关联部门主数据，必填）
     * 项目组成员（resource，关联员工主数据，多选）
     * 相关方（resource，关联客户主数据）
     * 项目发起人（resource，关联员工主数据）

2. 项目计划表格
   - 可添加多行记录，包含字段：
     * 任务名称（text，必填）
     * 任务描述（textarea）
     * 负责人（resource，关联员工主数据，必填）
     * 计划开始日期（date，必填）
     * 计划结束日期（date，必填）
     * 工作量（number，单位：人天）
     * 进度比例（slider，0-100）
     * 任务状态（select：未开始/进行中/已完成/暂停）
     * 前置任务（select，关联其他任务）
     * 相关文档（file）

3. 资源分配表格
   - 可添加多行记录，包含字段：
     * 资源类型（select：人力/设备/材料/其他）
     * 资源名称（text，必填）
     * 资源数量（number，必填）
     * 单位（text）
     * 计划使用时间（date）
     * 预计费用（number）
     * 实际费用（number）
     * 使用说明（textarea）
     * 资源状态（select：待分配/已分配/使用中/已归还）

4. 风险管理表格
   - 可添加多行记录，包含字段：
     * 风险描述（textarea，必填）
     * 风险等级（select：高/中/低）
     * 可能性（select：高/中/低）
     * 影响程度（select：高/中/低）
     * 应对措施（textarea）
     * 责任人（resource，关联员工主数据）
     * 计划完成日期（date）
     * 状态（select：未处理/处理中/已解决/已关闭）
     * 实际结果（textarea）

5. 项目审批流程
   - 部门经理审批
     * 审批人（resource，关联员工主数据）
     * 审批时间（datetime）
     * 审批结果（radio：同意/不同意/退回）
     * 审批意见（textarea）
     * 审批签名（signature）

   - PMO审核
     * 审核人（resource，关联员工主数据）
     * 审核时间（datetime）
     * 审核结果（radio：同意/不同意/退回）
     * 审核意见（textarea）
     * 审核签名（signature）

   - 分管领导审批
     * 审批人（resource，关联员工主数据）
     * 审批时间（datetime）
     * 审批结果（radio：同意/不同意/退回）
     * 审批意见（textarea）
     * 审批签名（signature）

6. 项目进度报告
   - 总体进度
     * 实际开始日期（date）
     * 实际进度（slider，0-100）
     * 预计完成日期（date）
     * 进度说明（textarea）
     * 存在问题（textarea）
     * 解决方案（textarea）
     * 相关附件（file）

   - 里程碑完成情况
     * 里程碑名称（text）
     * 计划完成日期（date）
     * 实际完成日期（date）
     * 完成状态（select：未开始/进行中/已完成/延期）
     * 完成说明（textarea）
     * 验收材料（file）

特殊功能支持：
1. 表单分组展示，使用 Tabs 切换
2. 支持表格数据的动态添加和删除
3. 支持文件上传和预览
4. 支持电子签名
5. 支持资源选择（关联主数据）
6. 支持数字字段的自动计算
7. 支持必填字段实时验证
8. 支持表单打印，生成标准化的 PDF 文档
9. 支持流程确认步骤的状态追踪
10. 支持表单数据的暂存和恢复
11. 支持甘特图展示
12. 支持项目进度的可视化
13. 支持风险矩阵展示
14. 支持资源使用统计
15. 支持项目文档管理


上述需求是否满足您的需求？如果不满足您可以告诉我您需要什么功能，我随时调整模板内容`

export default projectTemplatePrompt
