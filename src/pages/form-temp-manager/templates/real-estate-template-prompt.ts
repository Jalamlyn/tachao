/**
 * 房地产表单模板提示词
 * 包含房地产相关表单的标准化需求描述
 * 基于 DynamicForm 组件的实际功能进行优化
 */
export const realEstateTemplatePrompt = `这是一个用于房地产行业的看房预约、房屋验收等的综合性表单。

具体需求如下：

1. 客户信息分组
   - 基本信息
     * 客户姓名（text，必填）
     * 性别（radio：男/女）
     * 年龄（number）
     * 证件类型（select：身份证/护照/其他）
     * 证件号码（text，必填，支持验证）
     * 联系电话（tel，必填）
     * 电子邮箱（email）
     * 现居地址（textarea）
     * 客户来源（select：自然到访/中介推荐/广告推广/老客户介绍）
     * 意向等级（select：A/B/C/D）

   - 需求信息
     * 意向区域（select，多选）
     * 户型需求（select：一室/两室/三室/四室及以上）
     * 面积需求（select：50㎡以下/50-90㎡/90-120㎡/120㎡以上）
     * 价格预算（select：100万以下/100-150万/150-200万/200万以上）
     * 购房用途（select：自住/投资/其他）
     * 付款方式（select：全款/按揭/组合贷款）
     * 特殊需求（textarea）

2. 看房预约表格
   - 可添加多行记录，包含字段：
     * 预约编号（text，自动生成）
     * 预约日期（date，必填）
     * 预约时间（time，必填）
     * 项目名称（resource，关联项目主数据）
     * 户型（resource，关联户型主数据）
     * 置业顾问（resource，关联员工主数据）
     * 预约状态（select：待确认/已确认/已取消/已完成）
     * 到访人数（number）
     * 看房反馈（textarea）
     * 下次跟进（date）
     * 备注说明（textarea）

3. 房源信息表格
   - 可添加多行记录，包含字段：
     * 房源编号（text，自动生成）
     * 项目名称（resource，关联项目主数据）
     * 楼栋号（text）
     * 单元号（text）
     * 房号（text）
     * 户型（select）
     * 建筑面积（number）
     * 套内面积（number）
     * 朝向（select：东/南/西/北）
     * 楼层（number）
     * 总楼层（number）
     * 单价（number）
     * 总价（number，自动计算）
     * 装修状况（select：毛坯/简装/精装）
     * 房源状态（select：待售/已预订/已售）

4. 认购签约
   - 认购信息
     * 认购编号（text，自动生成）
     * 认购日期（date，必填）
     * 房源信息（resource，关联房源主数据）
     * 认购价格（number，必填）
     * 认购金额（number，必填）
     * 付款方式（select：全款/按揭/组合贷款）
     * 贷款银行（select，条件显示）
     * 贷款金额（number，条件显示）
     * 首付比例（number，条件显示）
     * 首付金额（number，自动计算）
     * 认购状态（select：待审核/已审核/已取消）

   - 合同信息
     * 合同编号（text，自动生成）
     * 签约日期（date）
     * 合同类型（select：预售合同/销售合同）
     * 网签时间（datetime）
     * 约定交房日期（date）
     * 合同附件（file）
     * 备注说明（textarea）

5. 房屋验收
   - 验收信息
     * 验收编号（text，自动生成）
     * 验收日期（date，必填）
     * 验收人员（resource，关联员工主数据）
     * 业主信息（resource，关联客户主数据）
     * 房屋信息（resource，关联房源主数据）
     * 验收类型（select：预验收/正式验收）
     * 验收状态（select：未验收/验收中/已通过/未通过）

   - 验收明细表格
     * 检查项目（text，必填）
     * 检查标准（textarea）
     * 检查结果（select：合格/不合格）
     * 问题描述（textarea）
     * 整改要求（textarea）
     * 验收照片（image，支持多图片）
     * 整改状态（select：待整改/整改中/已整改）
     * 复验结果（select：通过/不通过）
     * 备注说明（textarea）

6. 售后服务
   - 维修申请
     * 申请编号（text，自动生成）
     * 申请日期（datetime，必填）
     * 业主信息（resource，关联客户主数据）
     * 房屋信息（resource，关联房源主数据）
     * 问题类型（select：水电/墙面/地面/门窗/其他）
     * 问题描述（textarea，必填）
     * 现场照片（image，支持多图片）
     * 紧急程度（select：普通/紧急/特急）
     * 维修状态（select：待处理/处理中/已完成）

   - 维修记录
     * 维修日期（datetime）
     * 维修人员（resource，关联员工主数据）
     * 维修内容（textarea）
     * 维修材料（textarea）
     * 维修费用（number）
     * 维修照片（image）
     * 业主确认（signature）
     * 满意度评价（rate，1-5星）
     * 评价内容（textarea）

特殊功能支持：
1. 表单分组展示，使用 Tabs 切换
2. 支持表格数据的动态添加和删除
3. 支持图片上传和预览
4. 支持电子签名
5. 支持资源选择（关联主数据）
6. 支持数字字段的自动计算
7. 支持必填字段实时验证
8. 支持表单打印，生成标准化的 PDF 文档
9. 支持流程确认步骤的状态追踪
10. 支持表单数据的暂存和恢复
11. 支持房源信息管理
12. 支持客户跟进提醒
13. 支持验收项目管理
14. 支持维修工单管理
15. 支持统计报表导出

上述需求是否满足您的需求？如果不满足您可以告诉我您需要什么功能，我随时调整模板内容`

export default realEstateTemplatePrompt
