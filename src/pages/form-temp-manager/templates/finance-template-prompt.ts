/**
 * 财务报销表单模板提示词
 * 包含财务报销相关表单的标准化需求描述
 * 基于 DynamicForm 组件的实际功能进行优化
 */
export const financeTemplatePrompt = `这是一个用于财务报销和预算申请的综合性表单。

具体需求如下：

1. 申请人信息分组
   - 基本信息
     * 申请人（resource，关联员工主数据，必填）
     * 所属部门（resource，关联部门主数据，必填）
     * 申请日期（date，必填）
     * 报销类型（select，必填，选项：差旅费/办公费/业务招待费/其他）
     * 预算科目（select，必填）
     * 申请说明（textarea，必填）
     * 是否预支（switch）
     * 预支金额（number，条件显示）

2. 费用明细表格
   - 可添加多行记录，包含字段：
     * 费用类型（select，必填）
     * 发生日期（date，必填）
     * 费用金额（number，必填）
     * 发票类型（select：增值税专票/普通发票/电子发票）
     * 发票号码（text）
     * 发票图片（image，支持多图片）
     * 费用说明（textarea）
     * 所属项目（resource，关联项目主数据）
     * 核定金额（number，自动计算）

3. 差旅明细表格（差旅费用时显示）
   - 可添加多行记录，包含字段：
     * 出发地（text，必填）
     * 目的地（text，必填）
     * 出发时间（datetime，必填）
     * 返回时间（datetime，必填）
     * 交通工具（select：飞机/火车/汽车/其他）
     * 住宿费（number）
     * 交通费（number）
     * 餐饮费（number）
     * 其他费用（number）
     * 费用合计（number，自动计算）
     * 票据图片（image，支持多图片）

4. 预算信息分组
   - 预算控制
     * 预算年度（select，必填）
     * 预算期间（select，必填）
     * 预算科目（select，必填）
     * 可用预算（number，自动计算）
     * 已用预算（number，自动计算）
     * 本次申请（number，自动计算）
     * 剩余预算（number，自动计算）
     * 超预算说明（textarea，条件必填）

5. 审批流程步骤
   - 直接主管审批
     * 审批人（resource，关联员工主数据）
     * 审批时间（datetime）
     * 审批结果（radio：同意/不同意/退回）
     * 审批意见（textarea）
     * 审批签名（signature）

   - 财务初审
     * 审核人（resource，关联员工主数据）
     * 审核时间（datetime）
     * 审核结果（radio：同意/不同意/退回）
     * 核定金额（number）
     * 调整说明（textarea）
     * 审核签名（signature）

   - 财务经理审批（金额超过阈值时显示）
     * 审批人（resource，关联员工主数据）
     * 审批时间（datetime）
     * 审批结果（radio：同意/不同意/退回）
     * 审批意见（textarea）
     * 审批签名（signature）

   - 出纳支付确认
     * 出纳（resource，关联员工主数据）
     * 支付时间（datetime）
     * 支付方式（select：现金/转账/其他）
     * 支付金额（number）
     * 支付凭证（image）
     * 确认签名（signature）

6. 报销汇总信息
   - 费用统计
     * 报销总额（number，自动计算）
     * 核定总额（number，自动计算）
     * 预支金额（number，自动获取）
     * 应付金额（number，自动计算）
     * 大写金额（text，自动转换）

特殊功能支持：
1. 表单分组展示，使用 Tabs 切换
2. 支持表格数据的动态添加和删除
3. 支持发票图片的批量上传和预览
4. 支持电子签名
5. 支持资源选择（关联主数据）
6. 支持数字字段的自动计算
7. 支持必填字段实时验证
8. 支持表单打印，生成标准化的 PDF 文档
9. 支持流程确认步骤的状态追踪
10. 支持表单数据的暂存和恢复
11. 支持预算控制和超预算警告
12. 支持发票真伪验证
13. 支持金额大写自动转换
14. 支持预算使用统计
15. 支持费用明细导出


上述需求是否满足您的需求？如果不满足您可以告诉我您需要什么功能，我随时调整模板内容`

export default financeTemplatePrompt
