/**
 * 零售行业表单模板提示词
 * 包含零售行业相关表单的标准化需求描述
 * 基于 DynamicForm 组件的实际功能进行优化
 */
export const retailTemplatePrompt = `这是一个用于零售行业的会员管理、商品反馈和库存管理的综合性表单。

具体需求如下：

1. 会员信息分组
   - 基本信息
     * 会员编号（text，自动生成）
     * 会员姓名（text，必填）
     * 性别（radio：男/女）
     * 出生日期（date）
     * 手机号码（tel，必填）
     * 电子邮箱（email）
     * 居住地址（textarea）
     * 会员等级（select：普通/银卡/金卡/钻石）
     * 注册时间（datetime，自动生成）
     * 会员照片（image）

   - 会员卡信息
     * 卡号（text，自动生成）
     * 卡类型（select：实体卡/电子卡）
     * 开卡日期（date，必填）
     * 有效期（date）
     * 卡内余额（number）
     * 积分余额（number）
     * 卡片状态（select：正常/挂失/冻结/注销）

2. 消费记录表格
   - 可添加多行记录，包含字段：
     * 交易日期（datetime，必填）
     * 商品名称（resource，关联商品主数据，必填）
     * 商品类别（select，自动关联）
     * 购买数量（number，必填）
     * 单价（number，自动获取）
     * 折扣率（number）
     * 实付金额（number，自动计算）
     * 获得积分（number，自动计算）
     * 支付方式（select：现金/银行卡/微信/支付宝）
     * 销售员（resource，关联员工主数据）
     * 备注（textarea）

3. 商品反馈表格
   - 可添加多行记录，包含字段：
     * 反馈日期（datetime，必填）
     * 商品名称（resource，关联商品主数据，必填）
     * 购买日期（date）
     * 问题类型（select：质量/价格/服务/其他）
     * 问题描述（textarea，必填）
     * 图片附件（image，支持多图片）
     * 处理状态（select：待处理/处理中/已处理/已关闭）
     * 处理结果（textarea）
     * 处理人（resource，关联员工主数据）
     * 客户满意度（rate，1-5星）

4. 库存管理表格
   - 可添加多行记录，包含字段：
     * 商品编码（text，自动生成）
     * 商品名称（resource，关联商品主数据，必填）
     * 规格型号（text）
     * 库存数量（number，必填）
     * 库存单位（select）
     * 库存上限（number）
     * 库存下限（number）
     * 采购价（number）
     * 零售价（number）
     * 库存状态（select：正常/积压/短缺）
     * 存放位置（text）
     * 备注说明（textarea）

5. 促销活动管理
   - 活动信息
     * 活动编号（text，自动生成）
     * 活动名称（text，必填）
     * 活动类型（select：折扣/满减/赠品/积分）
     * 开始时间（datetime，必填）
     * 结束时间（datetime，必填）
     * 适用商品（resource，关联商品主数据，多选）
     * 活动规则（textarea，必填）
     * 优惠力度（number）
     * 活动海报（image）
     * 活动状态（select：未开始/进行中/已结束/已取消）

6. 销售统计分析
   - 销售数据
     * 统计周期（select：日/周/月/年）
     * 销售总额（number，自动计算）
     * 销售数量（number，自动计算）
     * 毛利润（number，自动计算）
     * 退货金额（number，自动计算）
     * 会员销售占比（number，自动计算）
     * 热销商品（resource，关联商品主数据）
     * 销售趋势（chart）
     * 会员分布（chart）

特殊功能支持：
1. 表单分组展示，使用 Tabs 切换
2. 支持表格数据的动态添加和删除
3. 支持图片上传和预览
4. 支持电子签名
5. 支持资源选择（关联主数据）
6. 支持数字字段的自动计算
7. 支持必填字段实时验证
8. 支持表单打印，生成标准化的 PDF 文档
9. 支持流程确认步骤的状态追踪
10. 支持表单数据的暂存和恢复
11. 支持会员积分计算
12. 支持库存预警
13. 支持销售统计
14. 支持数据可视化
15. 支持报表导出

上述需求是否满足您的需求？如果不满足您可以告诉我您需要什么功能，我随时调整模板内容`

export default retailTemplatePrompt
